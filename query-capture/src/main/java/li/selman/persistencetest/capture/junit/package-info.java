/** JUnit 5 integration: resets query capture state between tests and injects it into test methods. */
@NullMarked
package li.selman.persistencetest.capture.junit;

import org.jspecify.annotations.NullMarked;
